App.TodoItemController = Ember.ObjectController.extend({
    //deleteTodo: function (event) {
    //    var todoItem = this.get('content');
    //    var transaction = App.store.transaction();
    //    transaction.add(todoItem);
    //    todoItem.deleteRecord();
    //    transaction.commit();
    //},
});